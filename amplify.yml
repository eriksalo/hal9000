version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building frontend..."
        - echo "Frontend is static HTML/CSS/JS - no build needed"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
